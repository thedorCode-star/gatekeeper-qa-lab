name: k6 API Quality Gate

on:
 pull_request:
   branches:
     - main
 workflow_dispatch:

permissions:
  contents: read

jobs:
  performance-gate:
    name: QuickPizza API performance gate
    runs_on: ubuntu-latest

    steps:
     - name: Check out repository
       uses: actions/checkout@v4

     - name: Install k6
       uses: grafana/setup-k6-action@v1

     - name: Run k6 thresholds
       uses: grafana/run-k6-action@v1
       with:
         path: TODO
         fail-fast: true
